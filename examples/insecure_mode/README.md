Demo of using insecure mode of Centrifuge.

Note that to launch this demo you should first create new project in Centrifuge and change
`project` variable to your project ID value in `index.html` file.